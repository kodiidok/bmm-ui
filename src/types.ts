// src/types.ts

export interface Administrator {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
  }
  
  export interface Role {
    id: string;
    name: string;
  }